[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pansen-castlabs = {editable = true,path = ".",extras = ["testing"]}

[packages]
# TODO andi: what's up with the `yarl` issue? Seems not to be that one.
# https://github.com/pypa/pipenv/issues/1899#issuecomment-378466363
pansen-castlabs = {editable = true,path = "."}

[requires]
python_version = "3.8"
